import { BotonElementoPipe } from './boton-elemento.pipe';

describe('BotonElementoPipe', () => {
  it('create an instance', () => {
    const pipe = new BotonElementoPipe();
    expect(pipe).toBeTruthy();
  });
});
