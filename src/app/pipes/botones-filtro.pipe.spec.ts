import { BotonesFiltroPipe } from './botones-filtro.pipe';

describe('BotonesFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new BotonesFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
